<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.room">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".listViews.ListaLivroRecycle2"></activity>
        <activity android:name=".listViews.listGridView" />
        <activity android:name=".OpcoesListas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".listViews.PageAdapterController" />
        <activity android:name=".listViews.ListaLivroRecycle" />
        <activity android:name=".listViews.ListaLivros" />
        <activity android:name=".Cadastro" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>